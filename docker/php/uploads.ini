; Field photos from a real phone camera are often 3-8 MB; post_max_size must
; stay comfortably above upload_max_filesize to leave room for the rest of
; the multipart form (motif text, other fields, etc).
upload_max_filesize = 10M
post_max_size = 20M
